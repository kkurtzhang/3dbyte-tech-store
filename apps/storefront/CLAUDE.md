# Legacy Storefront Reference

This directory is not part of `pnpm-workspace.yaml` and is retained only as
reference code.

- The active customer app is `apps/storefront-v3`.
- Do not implement, build, test, or update dependencies here unless the user
  explicitly scopes the task to the legacy storefront.
- Do not copy framework examples from this directory without verifying them
  against the active app and its pinned dependencies.
- Apply the repo-root `AGENTS.md` if an explicitly authorized legacy task does
  require changes here.
