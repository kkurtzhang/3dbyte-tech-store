DATABASE_URL=postgres://postgres:postgres@localhost:5432/medusa_test
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
STORE_CORS=http://localhost:3000,http://localhost:3001
ADMIN_CORS=http://localhost:7001,http://localhost:9000
AUTH_CORS=http://localhost:3000,http://localhost:3001,http://localhost:7001,http://localhost:9000
REDIS_URL=redis://localhost:6379
JWT_SECRET=replace-with-a-test-only-jwt-secret
COOKIE_SECRET=replace-with-a-test-only-cookie-secret
AUTH_MFA_ENCRYPTION_KEY=replace-with-a-32-byte-test-only-encryption-key

# Server-side integration credentials must be injected by CI or the developer.
MEILISEARCH_HOST=http://localhost:7700
MEILISEARCH_BACKEND_API_KEY=replace-with-a-scoped-test-key
MEILISEARCH_PRODUCT_INDEX_NAME=products_test
MEILISEARCH_CATEGORY_INDEX_NAME=categories_test
MEILISEARCH_BRAND_INDEX_NAME=brands_test

STRAPI_API_URL=http://localhost:1337
STRAPI_API_TOKEN=replace-with-a-scoped-test-token
STRAPI_WEBHOOK_SECRET=replace-with-a-test-webhook-secret
